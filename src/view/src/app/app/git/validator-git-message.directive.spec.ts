import { ValidatorGitMessageDirective } from './validator-git-message.directive';

describe('ValidatorGitMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorGitMessageDirective();
    expect(directive).toBeTruthy();
  });
});
