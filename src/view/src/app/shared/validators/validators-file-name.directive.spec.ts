import { ValidatorsFileNameDirective } from './validators-file-name.directive';

describe('ValidatorsFileNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorsFileNameDirective();
    expect(directive).toBeTruthy();
  });
});
