import { ValidatorsBookFindDirective } from './validators-book-find.directive';

describe('ValidatorsBookFindDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorsBookFindDirective();
    expect(directive).toBeTruthy();
  });
});
