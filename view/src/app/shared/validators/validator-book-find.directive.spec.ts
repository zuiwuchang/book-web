import { ValidatorBookFindDirective } from './validator-book-find.directive';

describe('ValidatorBookFindDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorBookFindDirective();
    expect(directive).toBeTruthy();
  });
});
