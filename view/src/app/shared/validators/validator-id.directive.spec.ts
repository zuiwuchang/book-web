import { ValidatorIdDirective } from './validator-id.directive';

describe('ValidatorIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorIdDirective();
    expect(directive).toBeTruthy();
  });
});
